worker_id: w-local
coordinator_url: http://flow-server:8421
work_dir: /flow/work
labels:
  local: "true"
capacity:
  persistent_agent: 5
  ephemeral: 5
cleanup:
  interval: 5m
  orphan_grace: 1h
  min_free_percent: 10
  resume_free_percent: 15
git:
  principal: worker:w-local
