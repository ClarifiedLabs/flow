# Copy this file to .env before running Docker Compose.

# Harness model proxy running on the Docker host.
HARNESS_MODEL_PROXY_URL=http://host.docker.internal:8765

# Reusable secret that lets workers join the server and receive worker tokens.
# Generate with: openssl rand -hex 32
FLOW_WORKER_JOIN_TOKEN=

# Owner-scoped secret for human/admin CLI and web UI bootstrap.
# Generate with: openssl rand -hex 32
FLOW_OWNER_TOKEN=

# Non-root UID/GID used by the flow-server and flow-worker containers.
FLOW_UID=1000
FLOW_GID=1000

# Flow server URL reachable from the worker inside the Docker Compose network.
FLOW_WORKER_COORDINATOR_URL=http://flow-server:8421

# Rootless Docker daemon startup for the worker. Use false when binding an
# external Docker socket and setting DOCKER_HOST yourself.
FLOW_WORKER_DOCKERD=auto
FLOW_WORKER_DOCKERD_ARGS=

# Git commit identity. The server uses this for the squash-merge commit it
# creates (defaults to Flow Coordinator <flow@example.invalid> when unset); the
# worker injects it as GIT_AUTHOR_*/GIT_COMMITTER_* for agent commits. Both
# name and email must be set together or both left empty.
# Server: FLOW_GIT_COMMIT_NAME / FLOW_GIT_COMMIT_EMAIL
# Worker: FLOW_WORKER_GIT_COMMIT_NAME / FLOW_WORKER_GIT_COMMIT_EMAIL
FLOW_GIT_COMMIT_NAME=
FLOW_GIT_COMMIT_EMAIL=
FLOW_WORKER_GIT_COMMIT_NAME=
FLOW_WORKER_GIT_COMMIT_EMAIL=
